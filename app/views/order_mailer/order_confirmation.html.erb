<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h1>Order Confirmation</h1>
    <h3>Your Order #<%= @order.id %></h3>
    <p>
      <br>
    </p>
    <p>
      Hey <%= @user.first_name %>,

      Thanks for ordering with us!

    </p>
      <div class="panel panel-default items">
    <table class="table table-bordered">
      <thead>
        <tr>
          <th>Product</th>
          <th>Unit Price</th>
          <th>Quantity</th>
          <th>Price</th>
        </tr>
      </thead>
      <tbody>
      <% @items.each do |item| %>
      <tr>
        <td>
          <h4><%= item.product.name %></h4>
          <br>
          <p><%= item.product.description %></p>
        </td>
        <td>
          <%= item.product.price %>
        </td>
        <td>
          <%= item.quantity %>
        </td>
        <td>
          <%= item.product.price * item.quantity %>
        </td>
      </tr>
    <% end %>
      </tbody>
      <tfoot>
        <tr>
          <th colspan="4">
            TOTAL:
          </th>
          <th>
            <%= @items.sum(:total_price_cents) / 100.0 %>
          </th>
        </tr>
      </tfoot>
    </table>
  </div>


  </body>
</html>